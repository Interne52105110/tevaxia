import type { Metadata } from "next";
import { localizedAlternates } from "@/lib/seo";
export const metadata:Metadata={title:"E‑2 evidence file — principal investor | Tevaxia",description:"Prepare evidence for a principal E‑2 investor in the United States. Statuses describe your documentation, without an eligibility opinion or probability calculation.",alternates:localizedAlternates("/hotellerie/score-e2","en")};
export default function Layout({children}:{children:React.ReactNode}){return children;}
