import type { Metadata } from "next";
import { localizedAlternates } from "@/lib/seo";
export const metadata:Metadata={title:"Dossier documentaire E‑2 — investisseur principal | Tevaxia",description:"Préparer les pièces du dossier E‑2 d’un investisseur principal aux États-Unis. Les états ci-dessous décrivent votre documentation, sans calcul de probabilité ni avis d’éligibilité.",alternates:localizedAlternates("/hotellerie/score-e2","fr")};
export default function Layout({children}:{children:React.ReactNode}){return children;}
