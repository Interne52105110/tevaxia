import type { Metadata } from "next";
import { localizedAlternates } from "@/lib/seo";
export const metadata:Metadata={title:"Dossier documental E‑2 — investidor principal | Tevaxia",description:"Preparar documentos para um investidor principal E‑2 nos Estados Unidos. Os estados descrevem a documentação, sem parecer de elegibilidade nem cálculo de probabilidade.",alternates:localizedAlternates("/hotellerie/score-e2","pt")};
export default function Layout({children}:{children:React.ReactNode}){return children;}
