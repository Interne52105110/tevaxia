import {describe,it,expect} from 'vitest';
import {EMPTY_ESG,summarizeEsgDeclarations,calculateEsgSensitivity} from '../esg';
describe('Environmental dossier and explicit valuation sensitivity',()=>{
 it('keeps unknown declarations distinct from a documented absence and never creates ratings',()=>{const r=summarizeEsgDeclarations(EMPTY_ESG);expect(r.unknown).toBe(7);expect(r.documented).toBe(0);expect(r).not.toHaveProperty('impactValeur');expect(r).not.toHaveProperty('score');const s=summarizeEsgDeclarations({...EMPTY_ESG,esgZoneInondable:'no',esgPompeAChaleur:'yes'});expect(s.documented).toBe(2);expect(s.unknown).toBe(5)});
 it('applies only a supplied adjustment, with an explanation for non-zero scenarios',()=>{expect(calculateEsgSensitivity(1000000,-5,'Matched market comparables')).toEqual({change:-50000,adjustedValue:950000});expect(calculateEsgSensitivity(1000000,6,'Scenario sensitivity')).toEqual({change:60000,adjustedValue:1060000});expect(calculateEsgSensitivity(1000000,0,'')).toEqual({change:0,adjustedValue:1000000});expect(()=>calculateEsgSensitivity(1000000,6,' ')).toThrow()});
 it('rejects absent/invalid valuations and adjustments outside the stated range',()=>{for(const n of [0,-1,NaN,Infinity])expect(()=>calculateEsgSensitivity(n,0,'')).toThrow();for(const n of [-100.01,100.01,NaN,Infinity])expect(()=>calculateEsgSensitivity(1000000,n,'study')).toThrow();expect(calculateEsgSensitivity(1000000,-100,'stress')).toEqual({change:-1000000,adjustedValue:0})});
});
